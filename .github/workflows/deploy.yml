name: Deploy to GitHub Pages

on:
  push:
    branches: ["claude/fishing-game-pygame-LvHcl", "main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system fonts
        run: sudo apt-get install -y fonts-ipafont-gothic

      - name: Install Python dependencies
        run: pip install pygame numpy pygbag

      - name: Bundle font for web build
        run: |
          mkdir -p assets/fonts
          cp /usr/share/fonts/opentype/ipafont-gothic/ipagp.ttf assets/fonts/

      - name: Build web version with pygbag
        run: python -m pygbag --build --ume_block 0 main.py

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
